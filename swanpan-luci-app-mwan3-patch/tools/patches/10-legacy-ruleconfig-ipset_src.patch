--- usr/lib/lua/luci/model/cbi/mwan/ruleconfig.lua
+++ usr/lib/lua/luci/model/cbi/mwan/ruleconfig.lua
@@ -63,10 +63,22 @@
 o = s:option(Value, "timeout", translate("Sticky timeout"),
 	translate("Seconds. Acceptable values: 1-1000000. Defaults to 600 if not set"))
 o.datatype = "range(1, 1000000)"
 
+o = s:option(Value, "ipset_src", translate("IPset source"),
+	translate("Name of IPset rule."))
+o:value("", translate("-- Please choose --"))
+for _, z in ipairs(ipsets) do
+	o:value(z)
+end
+
+o = s:option(Flag, "ipset_src_local", translate("Allow router-originated traffic"),
+	translate("Match traffic generated by the router without requiring its source address to be in the source IPset. Forwarded traffic must still match the source IPset."))
+o.default = o.disabled
+o.rmempty = true
+
 o = s:option(Value, "ipset", translate("IPset"),
 	translate("Name of IPset rule. Requires IPset rule in /etc/dnsmasq.conf (eg \"ipset=/youtube.com/youtube\")"))
 o:value("", translate("-- Please choose --"))
 for _, z in ipairs(ipsets) do
 	o:value(z)
 end
