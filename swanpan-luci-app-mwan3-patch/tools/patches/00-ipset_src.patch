--- www/luci-static/resources/view/mwan3/network/rule.js
+++ www/luci-static/resources/view/mwan3/network/rule.js
@@ -105,6 +105,22 @@
 		o.depends('sticky', '1');

+		o = s.option(form.Value, 'ipset_src', _('IPset source'),
+			_('Name of IPset rule.'));
+		o.value('', _('-- Please choose --'));
+		let ipset_srcs = data[0].split(/\n/);
+		for (let ips of ipset_srcs) {
+			if (ips.length > 0)
+				o.value(ips);
+		}
+		o.modalonly = true;
+
+		o = s.option(form.Flag, 'ipset_src_local', _('Allow router-originated traffic'),
+			_('Match traffic generated by the router without requiring its source address to be in the source IPset. Forwarded traffic must still match the source IPset.'));
+		o.default = '0';
+		o.rmempty = true;
+		o.modalonly = true;
+
 		o = s.option(form.Value, 'ipset', _('IPset'),
 			_('Name of IPset rule. Requires IPset rule in /etc/dnsmasq.conf (eg "ipset=/youtube.com/youtube")'));
 		o.value('', _('-- Please choose --'));
